"use strict";
//use strict is used to avoid silly mistake by programmer side in JS 
// intro to variables

// variables can store some information
// we can use that information later
// we can change that information later
//variable are case-sensitive

// declare a variable 

var firstName = "Pratik";

// use a variable 
console.log(firstName);

// change value 
//we if decalare firstname(which is case sensitve) a new variable will get declared 

firstName = "Kashika";

console.log(firstName);